// state values available to components, retrieve on computed methods

export const myValue = state => {
  return state.myValue;
};

export const lang = state => {
  return state.lang;
};

export const queries = state => {
  return state.queries;
};
